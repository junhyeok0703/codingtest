#include <iostream>
#include <string.h>
#include <algorithm>
using namespace std;

int main(void){

    int N;
    cin >> N;
    
    int* Array = new int[N];

    for (int i = 0; i < N; ++i) {
       cin >> Array[i];
    }
    sort(Array,Array+N);
    
  
    int k =0;
    for(int a =2;a<N;a++){
        k=Array[a];
        int i = 0;
        int j = N-1;
        while(i<j){
            if(Array[+])
        }
    }


}